---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/layout/Header.astro';
import Footer from '../../components/layout/Footer.astro';
import Container from '../../components/ui/Container.astro';
import GlassCard from '../../components/ui/GlassCard.astro';
import Button from '../../components/ui/Button.astro';
import Animate from '../../components/react/ScrollAnimations';
import Gallery from '../../components/ui/Gallery.astro';

const gallery = [
  { src: '/images/recyclage-01.jpg', alt: 'Atelier de concassage', caption: 'Atelier de concassage' },
  { src: '/images/recyclage-02.jpg', alt: 'Atelier de concassage', caption: 'Atelier de concassage' }
];

const page = {
  title: 'Recyclage et valorisation',
  description: 'Puechoultres & Fils exploite un site de concassage à Baraqueville pour la revalorisation des déchets inertes : gravats, bétons, enrobés. Économie circulaire dans le BTP en Aveyron.',
  hero: '/images/hero-recyclage.jpg',
};

const prestations = [
  'Concassage de bétons et gravats',
  'Recyclage d\'enrobés bitumineux',
  'Revalorisation de terres et déblais',
  'Production de granulats recyclés',
  'Acceptation de déchets inertes de chantier',
  'Tri sélectif et séparation des matériaux',
  'Fourniture de graves recyclées',
  'Évacuation et transport de déchets inertes',
];

const autresServices = [
  { label: 'Démolition',        href: '/services/demolition' },
  { label: 'Désamiantage',      href: '/services/desamiantage' },
  { label: 'Travaux publics',   href: '/services/travaux-publics' },
  { label: 'Transport',         href: '/services/transport' },
  { label: 'Voirie et réseaux', href: '/services/voirie-et-reseaux' },
  { label: 'Location',          href: '/services/location' },
];
---

<BaseLayout title={page.title} description={page.description}>
  <Header />
  <main>

    <section class="relative h-72 sm:h-96 flex items-end overflow-hidden">
      <img src={page.hero} alt="Site de concassage et recyclage Puech Oultres" class="absolute inset-0 w-full h-full object-cover object-center" loading="eager" />
      <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-black/20" />
      <Container class="relative z-10 pb-8 sm:pb-12">
        <nav aria-label="Fil d'Ariane" class="mb-3">
          <ol class="flex items-center gap-2 text-sm text-gray-400">
            <li><a href="/" class="hover:text-white transition-colors">Accueil</a></li>
            <li aria-hidden="true" class="text-gray-600">/</li>
            <li><a href="/services" class="hover:text-white transition-colors">Services</a></li>
            <li aria-hidden="true" class="text-gray-600">/</li>
            <li class="text-white font-medium" aria-current="page">Recyclage</li>
          </ol>
        </nav>
        <h1 class="font-heading font-black text-3xl sm:text-5xl text-white leading-tight">Recyclage et valorisation</h1>
      </Container>
    </section>

    <section class="py-16 bg-gray-900">
      <Container>
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-10">

          <div class="lg:col-span-8 flex flex-col gap-12">
            <Animate animation="slideUp" client:visible>
              <div>
                <h2 class="font-heading font-bold text-2xl sm:text-3xl text-white mb-5">Notre expertise</h2>
                <div class="space-y-4 text-gray-300 leading-relaxed">
                  <p>La SARL Puechoultres & Fils a développé un concept novateur dans le secteur du BTP en exploitant un <strong class="text-white">site de concassage à Baraqueville</strong>, dédié à la revalorisation des déchets de chantier inertes.</p>
                  <p>Ce site permet de traiter les gravats, bétons et enrobés issus des chantiers de démolition et de travaux publics pour les transformer en granulats recyclés réutilisables. Cette démarche d'économie circulaire réduit les coûts d'évacuation et limite l'impact environnemental des chantiers.</p>
                  <p>Nous proposons également un service d'acceptation des déchets inertes pour les artisans et entreprises du BTP qui souhaitent valoriser leurs déchets de chantier dans le respect de la réglementation.</p>
                </div>
              </div>
            </Animate>

            <Animate animation="slideUp" delay={0.05} client:visible>
              <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
                {[
                  { title: 'Concassage', desc: 'Traitement mécanique des bétons, gravats et enrobés pour produire des granulats calibrés réutilisables en sous-couche.' },
                  { title: 'Valorisation', desc: 'Les matériaux recyclés sont réintégrés dans les chantiers de voirie, remblais et sous-fondations.' },
                  { title: 'Dépôt inerte', desc: 'Acceptation des déchets inertes de chantier pour les professionnels du BTP et les collectivités.' },
                ].map(c => (
                  <div class="p-5 rounded-card border border-white/10 bg-white/5">
                    <h3 class="font-heading font-semibold text-white mb-2">{c.title}</h3>
                    <p class="text-gray-400 text-sm leading-relaxed">{c.desc}</p>
                  </div>
                ))}
              </div>
            </Animate>

            <Animate animation="slideUp" delay={0.1} client:visible>
              <div>
                <h2 class="font-heading font-bold text-2xl sm:text-3xl text-white mb-6">Nos prestations</h2>
                <ul class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                  {prestations.map(item => (
                    <li class="flex items-start gap-3 text-gray-300">
                      <svg class="w-5 h-5 text-primary shrink-0 mt-0.5" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="20 6 9 17 4 12" /></svg>
                      <span>{item}</span>
                    </li>
                  ))}
                </ul>
              </div>
            </Animate>

            <Animate animation="slideUp" delay={0.15} client:visible>
              <div class="p-6 rounded-card border border-white/10 bg-white/5">
                <h3 class="font-heading font-semibold text-lg text-white mb-2">Site de Baraqueville</h3>
                <p class="text-gray-400 text-sm leading-relaxed">Notre plateforme de recyclage est située à Baraqueville (Aveyron). Ouverte aux professionnels du BTP et aux collectivités pour le dépôt de déchets inertes. Contactez-nous pour connaître les conditions d'accès et les tarifs.</p>
              </div>
            </Animate>
          </div>

          <aside class="lg:col-span-4">
            <div class="lg:sticky lg:top-24 flex flex-col gap-6">
              <GlassCard hover={false}>
                <h3 class="font-heading font-bold text-lg text-white mb-5">Besoin d'un devis ?</h3>
                <form name="devis-recyclage" method="POST" data-netlify="true" class="flex flex-col gap-4">
                  <input type="hidden" name="form-name" value="devis-recyclage" />
                  <input type="hidden" name="service" value="Recyclage" />
                  <div>
                    <label for="nom" class="block text-xs font-medium text-gray-400 mb-1.5">Nom *</label>
                    <input id="nom" name="nom" type="text" required placeholder="Votre nom" class="w-full rounded-btn bg-white/5 border border-white/10 px-3 py-2.5 text-sm text-white placeholder:text-gray-500 focus:border-primary focus:outline-none focus:ring-1 focus:ring-primary transition-colors" />
                  </div>
                  <div>
                    <label for="email" class="block text-xs font-medium text-gray-400 mb-1.5">Email *</label>
                    <input id="email" name="email" type="email" required placeholder="votre@email.fr" class="w-full rounded-btn bg-white/5 border border-white/10 px-3 py-2.5 text-sm text-white placeholder:text-gray-500 focus:border-primary focus:outline-none focus:ring-1 focus:ring-primary transition-colors" />
                  </div>
                  <div>
                    <label for="telephone" class="block text-xs font-medium text-gray-400 mb-1.5">Téléphone</label>
                    <input id="telephone" name="telephone" type="tel" placeholder="06 00 00 00 00" class="w-full rounded-btn bg-white/5 border border-white/10 px-3 py-2.5 text-sm text-white placeholder:text-gray-500 focus:border-primary focus:outline-none focus:ring-1 focus:ring-primary transition-colors" />
                  </div>
                  <div>
                    <label for="message" class="block text-xs font-medium text-gray-400 mb-1.5">Message *</label>
                    <textarea id="message" name="message" required rows="4" placeholder="Volume de déchets, type de matériaux…" class="w-full rounded-btn bg-white/5 border border-white/10 px-3 py-2.5 text-sm text-white placeholder:text-gray-500 focus:border-primary focus:outline-none focus:ring-1 focus:ring-primary transition-colors resize-none"></textarea>
                  </div>
                  <Button variant="primary" class="w-full justify-center">Envoyer ma demande</Button>
                </form>
              </GlassCard>
              <GlassCard hover={false}>
                <h3 class="font-heading font-semibold text-base text-white mb-4">Autres services</h3>
                <ul class="flex flex-col gap-1">
                  {autresServices.map(s => (
                    <li>
                      <a href={s.href} class="flex items-center gap-2 px-3 py-2 rounded-lg text-sm text-gray-400 hover:text-white hover:bg-white/10 transition-colors duration-[150ms]">
                        <svg class="w-3.5 h-3.5 text-primary shrink-0" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 18l6-6-6-6"/></svg>
                        {s.label}
                      </a>
                    </li>
                  ))}
                </ul>
                <div class="mt-4 pt-4 border-t border-white/10">
                  <a href="/services" class="text-sm text-primary hover:text-primary-light font-medium transition-colors">Voir tous les services →</a>
                </div>
              </GlassCard>
            </div>
          </aside>

        </div>
      </Container>
    </section>

    <Gallery images={gallery} title="Notre site de recyclage" />

    <section class="relative py-20 overflow-hidden" style="background: linear-gradient(135deg, #1F1F1F 0%, #3B0A0A 50%, #1F1F1F 100%);">
      <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-primary to-transparent" aria-hidden="true" />
      <Container class="relative z-10 text-center">
        <Animate animation="slideUp" client:visible>
          <h2 class="font-heading font-black text-2xl sm:text-4xl text-white mb-4">Des déchets à valoriser ?</h2>
          <p class="text-gray-300 text-lg max-w-xl mx-auto mb-8">Contactez-nous pour connaître nos conditions d'acceptation et de traitement.</p>
          <div class="flex flex-wrap justify-center gap-4">
            <Button href="/nous-contacter" variant="primary" size="lg">Contactez-nous</Button>
            <Button href="tel:0565690270" variant="outline" size="lg">05 65 69 02 70</Button>
          </div>
        </Animate>
      </Container>
    </section>

  </main>
  <Footer />
</BaseLayout>
